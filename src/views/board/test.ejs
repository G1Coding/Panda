여기가 아마 main 파일
main에서 썸네일 누르면 detail 페이지로 이동
<hr>

<!-- test.ejs -->

<h1>Detail Page</h1>
<ul>
    <% for (var i = 0; i < members.user_board.length; i++) { %>
        <li>
            <a href="/board/detail?num=<%= members.user_board[i].num %>" method="get">
                <%= members.user_board[i].title %>
            </a>
        </li>
    <% } %>
</ul>